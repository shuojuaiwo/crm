spring:
  datasource:
    #    驱动
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/tb_crm?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    password: 123456
    username: root
    #MQ消息队列的配置
  rabbitmq:
      host: localhost
      port: 5672
      username: guest
      password: guest
      #发邮件配置
  mail:
    default-encoding: UTF-8
    host: smtp.qq.com
    port: 587
    username: 1919906937@qq.com
    password: fiaubzledmwhbgjb
    properties:
      mail:
        smtp:
          auth: true
          timeout: 25000
  redis:
    port: 6379
    host: 127.0.0.1



#控制台打印sql
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  typeAliasesPackage: com.chen.removeguanc.chen.entity
  mapperLocations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      logic-delete-field: flag  # 全局逻辑删除的实体字段名(since 3.3.0,配置后可以忽略不配置步骤2)
      logic-delete-value: 1 # 逻辑已删除值(默认为 1)
      logic-not-delete-value: 0 # 逻辑未删除值(默认为 0)